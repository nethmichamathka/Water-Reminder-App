<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:background="@color/background_color"
    android:padding="24dp">

    <!-- Header text: SPLASH -->
    <TextView
        android:id="@+id/header_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="@string/header_text"
        android:textColor="@color/primary_text_color"
        android:textSize="36sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif"
        android:letterSpacing="0.1"
        android:gravity="center" />

    <!-- Water drop image with hands up -->
    <ImageView
        android:id="@+id/water_image"
        android:layout_width="400dp"
        android:layout_height="200dp"
        android:src="@drawable/ic_water_drop_character"
        android:contentDescription="@string/water_image_description"
        android:layout_marginBottom="24dp" />

    <!-- Subheading text: Drink More -->
    <TextView
        android:id="@+id/subheading_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/subheading_text"
        android:textColor="@color/primary_text_color"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <!-- Button: Let's Splash -->
    <Button
        android:id="@+id/button1"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/rounded_button"
        android:fontFamily="sans-serif"
        android:padding="16dp"
        android:text="@string/splash_button_text"
        android:textColor="@color/primary_text_color"
        android:textSize="18sp" />
</LinearLayout>
