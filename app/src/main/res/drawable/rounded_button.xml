<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#00BCD4" /> <!-- Button background color (Teal) -->
    <corners android:radius="20dp" /> <!-- Rounded corners -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
    <shadowColor android:color="#80000000" /> <!-- Optional shadow (slightly transparent) -->
    <shadowDx android:offsetX="4" />
    <shadowDy android:offsetY="4" />
    <shadowRadius android:radius="6" />
</shape>
